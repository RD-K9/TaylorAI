name: Deploy GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        working-directory: magic-markdown-fe
        run: bun install --frozen-lockfile

      - name: Build ATELIER SPA
        working-directory: magic-markdown-fe
        env:
          VITE_API_URL: ${{ vars.VITE_API_URL }}
          VITE_BASE: /TaylorAI/
        run: bun run build

      - name: Prepare Pages artifact
        working-directory: magic-markdown-fe
        run: |
          CLIENT="dist/client"
          ls -la "$CLIENT"
          test -f "$CLIENT/_shell.html"
          cp "$CLIENT/_shell.html" "$CLIENT/index.html"
          cp "$CLIENT/_shell.html" "$CLIENT/404.html"

      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: magic-markdown-fe/dist/client

      - id: deployment
        uses: actions/deploy-pages@v4
